#include <iostream>
using namespace std;

void print(int arr[], int n)
{
	for(int i=0;i<n;i++)
	{
		cout<<"arr["<<i<<"]= \t"<<arr[i]<<endl;
	}
}
/*Write a function called copy that takes an integer array, two indices
(source, destination) and (unsigned integer) count. The purpose of the function
is to copy a part of the array (start with index source) into another part of
the same array(start with index destination). */
void copy(int arr[], int n, int source, int destination, unsigned int count)
{
	//todo : complete the copy function
}